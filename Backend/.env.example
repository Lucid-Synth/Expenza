# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/expenza
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/expenza

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# Optional: Cross-Origin Configuration
CORS_ORIGIN=http://localhost:5173

# Optional: Email Configuration (if you implement email features)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_email_password